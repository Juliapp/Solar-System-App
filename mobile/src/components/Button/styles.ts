import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    display: 'flex',
    flex: 0,
    width: '50%',
    borderRadius: 8,
    paddingHorizontal: 32,
    paddingVertical: 16,
  },
});
